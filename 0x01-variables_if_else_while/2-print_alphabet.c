#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
*main - Print Alphabet
*Return: Always 0 (Success)
*/

int main(void)
{
	char ch = 'a';
	for( ; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	return (0);
}
